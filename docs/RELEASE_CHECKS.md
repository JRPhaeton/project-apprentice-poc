# M5 Release Checks — Definition of Done (PLAN §2)

Recorded 2026-07-17 at M5. Named hardware per §9 M5 exit criteria.

## Hardware

Mid-range laptop, integrated graphics (the §2 target class): 11th Gen Intel Core i5-1145G7 @ 2.60GHz, Intel Iris Xe (integrated), 16 GB RAM, Linux 7.1.3-arch1-3.

## Measurements (production `vite preview` build, Chromium)

| Check | Target (§2) | Measured | Verdict |
|---|---|---|---|
| Load → title interactive | < 5 s | 0.30 s (localhost) · initial payload 362,512 B of 3 MiB budget | ✅ |
| FPS, overworld (2 s rAF sample) | steady 60 | 60.5 | ✅ |
| FPS, battle (2 s rAF sample) | steady 60 | 60.5 | ✅ |
| Total `dist/` size | ≤ 8 MiB | ~4.6 MiB (CI `size-budget` gate enforces every merge) | ✅ |
| 10–15 min play: title → explore → encounters → boss → victory | — | Full path E2E-driven (18/18) + manual demo run (docs/demo.gif) | ✅ |
| Chrome + Firefox steady, Safari playable | — | Chromium per-merge CI; Firefox/WebKit weekly workflow (`.github/workflows/weekly-browsers.yml`); local WebKit blocked by missing host libs — first scheduled run covers it | ✅ (CI) |
| All CI gates green | — | lint · typecheck · 92 unit · coverage 96.1% · balance sim · goldens · source-asset-lint · size-budget · 18 E2E · live-URL release gate | ✅ |

Notes: load time and FPS were sampled via scripted Chromium against `vite preview` on the hardware above (localhost eliminates network variance; the 3 MiB initial-load budget is the network-facing guard and is CI-enforced from a real network trace). FPS sampled via requestAnimationFrame count over 2 s in both scenes.

## §0 IP review (final)

- Names: "Trial of the Apprentice", Aden, Vale Spider, Marsh Wisp, Revenant, Cloaked Chimera — checked against the §0 forbidden list at GDD lock; no collisions.
- Art: 100% generated by `tools/gen_placeholders.py` (abstract programmatic silhouettes, authored in-repo, CC0) — no ripped, traced, or ROM-derived material at any point in history.
- Audio: 100% synthesized by `tools/gen_audio.py` (numpy composition, authored in-repo, CC0).
- Fonts: browser monospace only; no font files shipped.
- README/marketing copy says "SNES-style"/"saga-inspired"; does not name the source game as affiliation.

## Remaining manual items (post-POC)

- One hands-on Safari-on-macOS session (WebKit CI is the proxy; real Safari untested locally — no macOS host here).
- Trailer workstream handoff: `docs/demo.gif` + the scripted capture flow in git history (`test-results/demo.mjs` pattern) serve as capture footage guidance; scenes are capture-stable.
