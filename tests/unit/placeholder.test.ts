import { describe, expect, it } from 'vitest';

// M0 placeholder (§9): proves the unit-test gate runs in CI.
// Replaced by real combat-core tests at M1.
describe('walking skeleton', () => {
    it('runs unit tests in CI', () => {
        expect(1 + 1).toBe(2);
    });
});
